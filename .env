"""# ===============================
# .env - Environment Variables
# ===============================

# Django Settings
DEBUG=False
SECRET_KEY=your-django-secret-key-here-generate-with-django
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Database
DB_NAME=restaurant_os_db
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# n8n Integration
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/ask-ai-SECRET-TOKEN-HERE
N8N_API_KEY=generate-secure-random-key-32-chars-minimum
N8N_REQUEST_TIMEOUT=120

# OpenAI (if calling directly from Django)
OPENAI_API_KEY=sk-your-openai-api-key

# CORS - Frontend URLs
FRONTEND_URL=http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-production-domain.com

# Security
CSRF_TRUSTED_ORIGINS=https://your-production-domain.com
SECURE_SSL_REDIRECT=False  # Set to True in production with HTTPS

# Email (for notifications)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Celery (if using async tasks)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO

# ===============================
# n8n Environment Variables
# (Set these in your n8n instance)
# ===============================

# DJANGO_API_URL=https://your-django-backend.com
# N8N_API_KEY=same-key-as-above
# OPENAI_API_KEY=sk-your-openai-api-key
# POSTGRES_HOST=your-postgres-host
# POSTGRES_DB=restaurant_os_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-db-password

# ===============================
# How to Generate Secure Keys
# ===============================

# Django SECRET_KEY:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

# N8N_API_KEY:
# python -c 'import secrets; print(secrets.token_urlsafe(32))'

# Or use:
# openssl rand -base64 32"""